package ru.nsu.fit.dubinskaya.Snake.Views;

import javafx.stage.Stage;

/**
 * This class is used to show snake's main menu on the stage
 */
public class MenuView extends View {
    /**
     * This is MainView constructor
     * @param primaryStage - stage where view should work
     */
    public MenuView(Stage primaryStage){
        setPrimaryStage(primaryStage);
    }
}
